export function useAuth() {
  // TODO: Replace with real auth state when backend/auth system is integrated
  const isAuthenticated = false; // Toggle to true to test LandingPageNavbarAuth
  return { isAuthenticated };
}
